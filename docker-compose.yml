services:
  killed-by-laravel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: killed-by-laravel-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production